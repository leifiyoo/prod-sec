interface:
  display_name: "ProdSec"
  short_description: "Defensive secure code review"
  brand_color: "#111827"
  default_prompt: "Use $prod-sec to review my local repository for security issues and generate a prioritized findings report."

policy:
  allow_implicit_invocation: true
