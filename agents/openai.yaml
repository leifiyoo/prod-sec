interface:
  display_name: "ProdSec"
  short_description: "Authorized active security auditing"
  brand_color: "#111827"
  default_prompt: "Use $prod-sec to run an authorized safe-mode security audit of my scoped web app and generate a findings report."

policy:
  allow_implicit_invocation: true
