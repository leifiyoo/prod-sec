# Codex Instructions: prod-sec

Read `SKILL.md` first. Treat this repository as an authorized security auditing skill with safe-mode-first active validation.

Use `references/` progressively and run `scripts/` only against explicitly scoped targets. Do not add or execute behavior for unauthorized access, persistence, evasion, malware, credential theft, destructive testing, or real data exfiltration.
